# experiments/results

Status: `planned` (Phase 5+).

Each run writes to `experiments/results/<date>-<experiment>/` with `README.md`, `metrics.json`, `config.yaml`. Never overwrite prior results. Directory is otherwise ignored by git (see `.gitignore`).
