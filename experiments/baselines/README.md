# experiments/baselines

Status: `planned` (Phase 4).

Future baseline definitions live here (one subdirectory per baseline, each with `README.md` + config). No baselines implemented yet.

Planned baselines:
- major-version change heuristic
- direct-dependency heuristic
- API-change heuristic
- dependency-depth heuristic
