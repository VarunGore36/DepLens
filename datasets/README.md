# Datasets

Status: `planned` (Phase 3).

No data collected yet. Each dataset entry must document:

- source repositories + commit SHAs
- collection script / command
- labeling method (CI status, test outcomes, revert commits)
- provenance + license
- date collected + DepLens version

Raw dumps (`raw/`, `*.sqlite`, `*.parquet`) are git-ignored; only documentation and collection code are versioned.
