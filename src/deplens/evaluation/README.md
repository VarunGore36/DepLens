# `evaluation` — Status: `in progress` (Phase 5)

`metrics.py` provides binary classification metrics (precision, recall, F1, FP/FN rates, accuracy) and Brier score for calibration. `scoring.py` scores every baseline rule against one labeled case list for head-to-head comparison. Temporal splits, ablations, per-stratum reporting — `planned` per `docs/methodology.md`.
