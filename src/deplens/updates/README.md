# `updates` — Status: `in progress` (Phase 3)

`detection.py` mines dependency bumps from git history by diffing parsed dependency files per commit (`UpdateRecord` with old/new constraints, added/removed/updated). `labeling.py` assigns heuristic ground-truth labels (`reverted` / `fix-suspect` / `no-signal`) from later revert and fix commits. `test_outcomes.py` runs a caller-supplied test command in isolated worktrees at parent and update commits, labeling `breaks-tests` / `passes` / `already-failing`, with `no-tests` for undecidable exit codes (e.g. pytest's 5), `error` for infrastructure failures, and `timeout` for overruns. The `*_isolated` path additionally builds a fresh venv per commit, installs the worktree's requirements plus declared test extras (`test`/`tests`/`testing`/`dev`) and the pytest runner — respecting era pins rather than overriding them. Trial-validated on a real `httpx` update — which also showed the limit: undeclared CI-only test deps (e.g. `anyio`) still surface as `already-failing`. Known deeper limit (proven on `urllib3`): partial era pins let unpinned transitive deps float to modern versions and break old suites (era Quart vs latest Flask); only full closures (`uv.lock` era) reproduce faithfully.
