# `graph` — Status: `in progress` (Phase 1)

Direct/transitive dependency graph: version constraints, depth, dependents/dependencies, serializable snapshot. `features.py` adds fan-in/fan-out and degree centrality for RQ2. Live transitive resolution via package metadata — `planned`.

See `docs/roadmap.md`.
